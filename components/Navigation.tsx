interface NavigationProps {
  brand?: any
  sticky?: any
}

export default function Navigation({ brand, sticky }: NavigationProps) {
  return (
    <div className="bg-white shadow-sm border-b mb-8">
      {/* Navigation component content */}
    </div>
  )
}